// Theme is now driven by CSS custom properties in styles.css via data-theme attribute.
// This stub exists so any stale imports don't break.
export default () => ({});
export const dark  = {};
export const light = {};
