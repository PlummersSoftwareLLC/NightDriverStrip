[audit]
# Optional local override for tools/pio_audit.py.
# Copy this file to config/audit.ini to activate it.

# Default behavior (without config/audit.ini) is soft-fail.
# Set to true to make audit violations fail the build.
hard_fail = true
